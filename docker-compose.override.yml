# Development overrides - exposes ports for local debugging
services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile.dev
    ports:
      - "8000:8000"

  martin:
    ports:
      - "3000:3000"
    networks:
      - backend
      - frontend
