# Development Dockerfile for Docusaurus
# Enables hot-reload for local development

FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy source files (will be overridden by volume mount)
COPY . .

# Expose port
EXPOSE 3000

# Start development server with host binding for Docker
CMD ["npm", "start", "--", "--host", "0.0.0.0"]
